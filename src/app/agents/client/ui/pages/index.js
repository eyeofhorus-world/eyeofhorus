
import notFound from './not-found';
import redirectToNotFound from './redirect-to-not-found';

import home from './home';

export default {
  routes: [
    home,

    notFound,
    redirectToNotFound,
  ],
};
